{
  "name": "estar-booking-engine",
  "version": "1.0.0",
  "description": "Hotel Estar booking engine assets and build scripts.",
  "scripts": {
    "build": "node build.js",
    "dev": "esbuild motor-app.jsx --outfile=motor-app.js --watch"
  },
  "dependencies": {
    "@netlify/blobs": "^8.0.0"
  },
  "devDependencies": {
    "esbuild": "0.20.2",
    "sharp": "^0.34.5",
    "ttf2woff2": "8.0.1"
  }
}
